/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class', // opcional, por si usas modo oscuro por clase
  content: [
    "./src/**/*.{html,ts}",    // plantillas y templates inline
  ],
  theme: {
    extend: {},
  },
  plugins: [
  ],
}
